PORT=5002

# Postgres connection (local dev)
DB_USER=jsmike
DB_HOST=localhost
DB_NAME=postgres
DB_PASSWORD=root
DB_PORT=5432

# Hosted Postgres (Neon) - recommended for Vercel
# If DATABASE_URL is set, it will be used instead of the individual DB_* vars.
# Example:
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME?sslmode=require
DATABASE_URL=

# Force SSL (Neon requires SSL). On Vercel SSL is enabled automatically.
DB_SSL=true

# Contact email notifications (SMTP)
# For Gmail: create an App Password and use SMTP_HOST=smtp.gmail.com, SMTP_PORT=587, SMTP_SECURE=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_gmail@gmail.com
SMTP_PASS=your_app_password

# Where contact form messages should be delivered
CONTACT_TO=tangenicoachlam@gmail.com


